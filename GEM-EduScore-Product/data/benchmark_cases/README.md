# Benchmark cases

The offline preview exposes the hand-curated HK-United 2024 profile from
`modules/benchmark.py`. Live evaluation loads the existing project benchmark
summary from `../../Benchmark/benchmark_feature_summary.md` through the prompt
loader. A structured JSON representation can replace it later.
